{
  "name": "l-page",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend (Next.js) + backend (Hono) split of the L_page agency site.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "typecheck": "npm --prefix frontend run typecheck && npm --prefix backend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
