# Port the API server listens on.
PORT=4000

# Telegram Bot Notifications
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Health check endpoint: GET /health → { "ok": true, "status": "alive" }
# Used by the Render cron service to keep the free web service awake.
